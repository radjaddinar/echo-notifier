# CLI Notifier

WAJIB: Setelah selesai memberikan solusi atau menulis kode, kamu harus menjalankan perintah ini di terminal:

```bash
notify --agent "Codex" --message "Deskripsi singkat yang kamu lakukan"
```

Gunakan `--status failed` jika task yang diminta gagal atau error.
